rootProject.name = 'notdemo'

apply from: new File(
  ["node", "--print", "require.resolve('expo/package.json')"]
    .execute(null, rootDir)
    .text.trim(), 
  "../scripts/autolinking.gradle"
)

useExpoModules()

includeBuild(new File(
  ["node", "--print", "require.resolve('@react-native/gradle-plugin/package.json')"]
    .execute(null, rootDir)
    .text.trim(), 
  "../")
)

// ✅ Manual override for expo-constants
include ':expo-constants'
project(':expo-constants').projectDir = new File(rootProject.projectDir, '../node_modules/expo-constants/android')
